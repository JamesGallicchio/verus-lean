# Dalek benchmarks B1–B5 — translation bugs & StrataBoole gaps

Status of the five dalek-lite benchmark targets (see `Strata/docs/BooleBenchmarks.md`)
through the Verus → VLIR → verus-lean → Boole → Strata pipeline, as of 2026-06-15.

Re-validated 2026-06-15 on `Strata` branch `pr/casts-boole` @ `fff49d4e3` (the
StrataBoole type-arg read-back fix below is carried in the working tree): the
working-suite gate holds 46 ✅ / 2 skips / 1 fail (`crypto_noref`), and every B1–B5
type-check / generation state below reproduces.

2026-06-14: **B5 now type-checks** — the trait associated-type projection blocker
is resolved in the translator (fixed-bugs table). `check_working_tests.sh`
(`SKIP_STRATA_VERIFY=1`) still reports "Generation: all working tests passed"; the
Strata type-check of `b1_minimal` / `b1_boundary_proved` is unchanged. The
resolution map also applies to existing `View::V` / `DeepView::V` projections in
the BST-map and exec-spec examples; those remained stable in the all-suites refresh.
B5 verification now reaches cvc5 and confirms the operator-dispatch faithfulness
gap in the open-gaps B5 row.

Target files: `tests/VerusFiles/{b1_minimal, b1_boundary_proved,
b1_full, b2_minimal, b3_minimal,
b4_minimal, b5_minimal}.rs` — all verify under our Verus fork
with 0 errors. `b1_full` is fully proved (zero admits — its whole
lemma closure is vendored from dalek-lite); the other targets keep
heavy/curve lemmas admitted as `/// TRUSTED AXIOM` stubs, per file headers.

## Pipeline status at a glance

| B | Target | Verus | Boole export | Strata type-check | Verify |
|---|--------|-------|--------------|-------------------|--------|
| 1 | `b1_minimal` (2 admits) | ✅ | ✅ | ✅ | z3 2026-07-07 **336/397 (61 ⌛, 0 ❓, 0 ❌)** with the sound emission set (`mul_return_ret_len` now guarded by the params' length facts); same obligation set as the 2026-07-04 run — the ⌛ delta is solver wall-clock (the run shared the machine with builds). Older: z3 2026-07-04 **348/397 (49 ⌛)** with the unguarded ret_len axiom; all non-pass are nonlinear `Impl__3_mul` timeouts. Older: cvc5 **395/437 (42 ⌛)** with the honest emission + hand-applied ambient facts (`tests/BoolePrograms/scratch/b1_minimal_fixed.lean`; baseline without them: 189/344): residue = ~28 nonlinear overflow/proof asserts in `Impl__3_mul` + 5 `mul_return`-length selects + 3 post-WF, all 10s timeouts. Quantified `mul_return`-length axiom regressed (390/47 — instantiation noise flips borderline obligations); 60s cvc5 + grind-backend runs staged in `scratch/` but unfinished. Older: cvc5 157/282; cslib Lean backend 61/70 (old emission) |
| 1 | `b1_boundary_proved` (1 admit) | ✅ 22/0 | ✅ | ✅ | z3 2026-07-07 **466/552 (86 ⌛, 0 ❓, 0 ❌)** with the sound emission set (same obligation set; ⌛ delta is machine contention). Older: z3 2026-07-04 **488/552 (64 ⌛)**; non-pass = nonlinear timeouts. |
| 1 | `b1_full` (0 admit) | ✅ 36/0 | ✅ | ✅ (elaborates with `lean -s 131072`) | z3 2026-07-07 **1095/1237 (142 ⌛, 0 ❓, 0 ❌)** with the sound emission set (same obligation set; ⌛ delta is machine contention). Older: z3 2026-07-04 **1133/1237 (104 ⌛)** — the `low_bits_mask_51_val` ground axiom closed the last ❓; non-pass = nonlinear `Impl__3_mul` timeouts. |
| 2 | `b2_minimal` (typesyn) | ✅ | ✅ | ✅ | z3 2026-07-07 **191/191** (the 3 synthesized array-param length clauses add 3 obligations over the earlier 188, all discharge). Older: gen_smt_vcs + `grind\|omega\|smt` closes all VCs; cvc5 `Boole.verify` 330/366 (2 ⌛) |
| 3 | `b3_minimal` | ✅ 17/0 | ✅ | ✅ **fully clean** (2026-06-10: translator fixes — tuple chains, struct destructors, shard dedupe, `Tuple2` rename for the cvc5 builtin; StrataBoole type-arg read-back fix) | z3 2026-07-06 **672/674 (2 ⌛, 0 ❓, 0 ❌)** with the sound emission set — obligations grew 450→674 because boundary threading mints per-call length obligations in place of the removed global axioms (all discharge), and the 12 `Impl__11_decompress_post_*` ❓ the removal exposed are closed by guarded enum-payload facts (`isOption_option_Some(r) ==> length(…) == N`). Older: z3 2026-07-03 **448/450 (2 ⌛, 0 ❌)** — the definition-level length class is closed by the auto-emitted `Sequence.length` contracts (spec-fn/proof-fn param `requires`, selector-path boundary facts, tuple/mut-out threading); the 2 ⌛ are heavy semantic obligations (`assert_53`, `Impl__11_decompress_ensures_44`). Older: cvc5 **430/509 (79 ⌛)** re-validated 2026-06-10 on `pr/casts-boole` @ `fff49d4e3`, ⌛ = the then-unfixed definition-level class (32× `u8_32_as_nat` `Sequence.select` bounds, `p`/`field_canonical` pow2 facts) |
| 4 | `b4_minimal` | ✅ 31/0 | ✅ | ✅ **fully clean** (2026-06-11: `choose`-defined spec fn `u8_32_from_nat` emits Boole's native `command_choosefndef` — Strata #1365, see fixed-bugs table; the tuple/struct errors had cleared with the B3 fixes) | z3 2026-07-06 **963/1009 (46 ⌛, 0 ❓, 0 ❌)** with the sound emission set — obligations grew 797→1009 (boundary threading, as in B3); the ⌛ growth is the new boundary obligations landing on the nonlinear field-arithmetic chain plus machine contention. Older: z3 2026-07-03 **785/797 (12 ⌛, 0 ❌)** — length-definedness classes closed (same emission set as the B3 row); the 12 ⌛ are the pre-existing nonlinear field-arithmetic `assert_*` chain. Older: cvc5 **532/667 (135 ⌛, 0 ❌)** with the native choose encoding — the compress proof chain (spec-equality cascade incl. `u8_32_from_nat`) passes; the ⌛ are the definition-level guard class (32× `u8_32_as_nat` selects, 37× inside the let-expanded `nat_invsqrt`, `p`/`field_canonical` sub/mod guards + call-site echoes) shared with B1/B3. (The earlier translator-emitted guarded axiom gave an equivalent 531/667 at 10s — same obligation set and timeout families; the native construct is now the sole encoding.) Native lowering's axiom is unguarded — sound here because every `n mod 2^256` has a 32-byte witness; the general guard is requested upstream on #1365. Batch-log caveat: timeouts print twice (dbg_trace + structured result) — count structured `Result:` lines only. |
| 5 | `b5_minimal` | ✅ 16/0 | ✅ | ✅ **type-checks** (2026-06-14: trait associated-type projection `<&MontgomeryPoint as Mul<&Scalar>>::Output` now resolves to the concrete impl type `montgomeryPoint` — see fixed-bugs table) | z3 2026-07-06 **289/290 (1 ❓, 0 🚨, 0 ❌)** — unchanged under the sound emission set (the sole ❓ remains `clamp_integer`'s semantic bit/sequence ensures). Same numbers 2026-07-04 with the full emission set: the trait-call resolution pass (abstract generic `Ops_Arith_Mul_mul` / `Std_specs_Ops_*` are not emitted; `Impl__12_mul_req`'s body lets `mul_clamped` discharge `Impl__13_mul`'s requires — see fixed-bugs table), the exec array-param `ensures length(p)==N` (the `clamp_integer`/`mul_clamped` post-definedness cluster), `bits_be_as_nat`'s recommends emitted as its domain `requires` (all call sites discharge them), and `spec_clamp_integer`'s 32-byte `*_ret_len` axiom.  The sole residual ❓ is `clamp_integer`'s semantic bit/sequence ensures.  Older: z3 2026-07-03 278/281 (3 ❓) mid-fix-stack; cvc5 **241/329 (1 🚨, 87 ⌛)** with the abstract-dispatch encoding error (`Ops_Arith_Mul_mul_ensures_8_3047`) |

## Translator (verus-lean) bugs found and fixed

The fixes span `VerusLean/VLIR/{Parser,Defs}.lean` and
`VerusLean/VLIR/Boole/{Translate,Inference,Context,Normalize,Pruning}.lean`; the
working-suite gate as of 2026-07-07 holds at 66 ✅ / 0 ❌ (3 Strata-gap +
8 solver-unknown skips), generation all-pass.

| Bug | Surfaced by | Fix |
|-----|-------------|-----|
| u128 emitted as `bv128` → Strata `bvTy: unsupported width 128` panic | B1 | Rule ①: `numKindOfTyp?` total — unsupported-width ints classify (and emit) as `int` |
| Numeric casts (widen/narrow/as-int) handled by scattered patches | B1 | Rule ③: one unified `Clip` rule |
| Int-modeled `x >> k` / `x << k` has no bv form | B1 | Rule ④: lower to `x div 2^k` / `x * 2^k` (constant `k`, value operand int-modeled) |
| Shift-amount width mismatch (`bv64 << bv32` rejected by Strata) | B1 | Pin amount width from the value operand's type |
| Rule ④ gate missed nested/literal shifts (`(x<<51)>>51`, `1u128<<108`) | B1 boundary | `shiftValueIntModeled?` — recurses through arith/bitwise binaries, reads literal types; kept separate from the coercion path |
| u128 *variable* not classified int ⇒ `u128 + bv64` mix undetected ⇒ bv64 const left uncoerced in int context | B1 boundary | `inferNumKind` routes through `numKindOfTyp?` |
| u128-typed / ≥2⁶⁴-valued literals contributed nothing to the arith footprint | B1 boundary | `arithFootprint` `.Const`: unsupported-width type or value ≥ 2⁶⁴ → `hasMathInt` |
| Bitwise binaries opaque to the arith footprint (un-lowered `u128 << k` hid the mix) | B1 boundary | `arithFootprint` recurses into `.Binary (.Bitwise …)` like `.Arith` |
| Single-field array struct lowered to opaque datatype + global `∀ length == N` axiom — **unsound** (asserts every `Sequence T` has length N) | B1/B2 | Wrapper-datatype trick: transparent `type <dt> := Sequence T` + identity ctor with `requires length == N` + identity destructor |
| `nat % nat` previously routed via precondition-free `nat.fromIntAux` — underspecified `mod 0` | B1 | `nat.mod` mirrors `nat.div` (guarded nonzero-divisor definition in `prelude/Nat.boole.st`); EuclideanMod fires on nat operand types |
| Comparison result inherited operand bv width ⇒ `(u == 1u8) == choice_is_true(c)` mis-coerced bool to int | B3 | `inferBitInfo`: `.Eq`/`.Ne`/`.Inequality` → `none` (a comparison is `bool`) |
| nat-arith results (`nat.mod` etc.) returned uncoerced ⇒ `(n % 2) as u8` and `n % 2 == 0` type-error | B3, B4 | Coerce the nat-domain result to the caller's expected kind (`nat.toInt` wrap) |
| Bare polymorphic `Sequence.empty` unparseable for type-variable element types | crypto_noref | `Bld.seqEmpty` fallback: emit `Sequence.empty<T>()` via Core `seq_empty` |
| Variable-amount shift on int-modeled u128 (`x >> shift` with `shift` a parameter, vstd `lemma_u128_shr_is_div`) → `bvTy: unsupported width 128` PANIC in `bvUShr`; recovered emission left `x >> shift` on `int` operands — invalid Boole | b1_full | Rule ④ variable-amount arm: `x >> e` → `x div int_pow2(e)`, `x << e` → `x * int_pow2(e)` (`intShiftViaPow2`); `int_pow2` + guarded axioms live in `prelude/Nat.boole.st` |
| Arith op reaching the bv-inference path with no inferred width lowers in the int domain but translated operands with `expected = none` ⇒ nat-returning call fed bare to int `div` (`x div pow2(…)` with `x : u128`) | b1_full | Operands of a no-bv-width arith op translate at `Int`, so nat leaves coerce via `nat.toInt` |
| Comparison between two arith trees got no numeric-kind signal (`inferComparableTyp?` has no `.Binary` case) ⇒ sides picked domains independently: `nat.mod(...) == (...) mod nat.toInt(p)` — nat vs int under `==` | b1_full (`lemma_u64_5_as_nat_product` ensures); repro: `tests/VerusFiles/repros/eq_clip_nat_mod_repro.rs` | `inferComparisonNumKind` joins an arith tree's operand kinds (nat⊔nat = nat, int involvement = int, bv defers to bit-info), so `comparisonPrelude` reconciles both sides to int |
| Rule ③ elided *narrowing* casts in int-modeled contexts — `(x : u128) as u64` emitted as bare `x` — collapsing vstd `lemma_cast_then_mod_51`'s ensures to the tautology `x mod c == x mod c`; same class silently mis-read `u64→u32` and `u64→i64` casts under int contexts | b1_full faithfulness review; probe: `tests/VerusFiles/repros/clip_gate_probe.rs` | Narrowing gate (`clipWrapIntModeled` + `fixedWidthInfoOfTyp`/`fitsFixedWidth`): a Clip to fixed width `w` in the int domain emits `mod 2^w` (signed targets: centered `(x + 2^(w-1)) mod 2^w − 2^(w-1)`) unless the inner's source type syntactically fits the target; widening stays elided; bv-context cast paths unchanged (native `as_bv<w>` already truncates) |
| Exec overflow VCs dropped: Verus materializes them as `assert HasType(τ, e)` ("possible arithmetic underflow/overflow", 31 in b1_full's exec body), and the Assert/Assume arms blanket-dropped every `HasType` expression — Boole-verified exec code carried no overflow obligations (bv ops wrap silently) | b1_full faithfulness review (soundness item S1) | `hasTypeRangeCond`: numeric `HasType(τ, e)` lowers to the range predicate `lo_τ ≤ e ≤ hi_τ` over the int model, for asserts (the overflow obligation) and assumes (the checked-range fact, also emitted by Verus for call results — partially restoring rule ①'s dropped range invariants); non-numeric `HasType` stays dropped as a typing tautology |
| `assert X; assume X` pairs from Verus's SST had the assume dropped as an "echo" (`isAssertAssumeEcho`) — but Strata's `assert` defers the obligation *without* extending path conditions (`Imperative/CmdEval`), so the echo assume is what carries the checked fact to later obligations | b1_full faithfulness audit (assert-fact persistence) | Echo suppression removed; Verus's Hoare-style pairs translate whole |
| N-tuple (size ≥ 3) projections/ctors emitted flat `Tuple_{size}_{k}` / `Tuple_ctor_{size}` names that nothing declares — tuple *types* lower to right-nested binary pairs, so the flat selectors could never exist | B3 (`Unknown variable Tuple_4_2`) | `tupleProjChain`/`tupleCtorChain`: field `k` of a `size`-tuple is `Tuple.._0` after `k` hops of `Tuple.._1` (last field: bare `.._1` spine); ctors right-nest `Tuple_ctor_2`; size-2 output unchanged byte-for-byte |
| Tuple-struct (positional-field) projections emitted the enum-style mangled destructor (`compressedEdwardsY..compressedEdwardsY_CompressedEdwardsY_0`) while the declaration names it `compressedEdwardsY.._0` — `projFieldNameOf`'s numeric branch lacked the variant==datatype struct special case the named branch has | B3 (`Unknown variable compressedEdwardsY..…_0`) | Numeric branch returns the bare field for struct variants (`Names.lean`); enum declarations bind fields through the same fn, so enums are unaffected |
| Multi-shard JSON bundles (impl + inner module) re-declared datatypes — `cmdDeclName?` returned `none` for `command_datatypes`, so `dedupeNamedCommands` passed duplicates through (`Type 'choice' is already declared`) | B3 (unmasked once the name errors were fixed) | `cmdDeclName?` names singleton-datatype commands (the only shape the translator emits), letting shard dedupe drop re-declarations |
| Support tuple datatype named `Tuple` — a cvc5 **builtin sort**: the emitted `(declare-datatype Tuple (par …))` parses but sort applications resolve to cvc5's native tuple, so selector applications fail every obligation (`matching failed for selector argument of parameterized datatype`); invisible until B3 became the first program to reach cvc5 with tuples | B3 (unmasked once the StrataBoole type-arg fix let B3 reach the solver) | Support datatype renamed `Tuple2` (`Tuple2_ctor_2`, `Tuple2.._0/_1`), centralized in `Names.lean` with the rationale; user datatypes can't collide (parsed names are decapitalized) |
| A tuple `IsVariant(x, tuple%N)` test is parsed as `Proj' N N` (the field index is read off the `tuple%N` variant string = the arity), which `tupleProjChain`'s bounds check rejected as out-of-range — generation aborted (`tuple projection out of range: field 2 of a 2-tuple`) | B5 (only Dalek target with a tuple variant test) | The `.Proj'` arm emits `boolConst true` when `field == size`: a tuple is single-constructor, so its variant test is always true (the projection encoding is a parser artifact). Inert for every other program — no working test has a tuple `IsVariant` |
| A spec fn whose whole body is `choose \|v\| pred` had the binder erased (`[TRANS-choose]` fallback translates the body verbatim), leaving a free `b` — `Unknown expr identifier b` | B4 (`u8_32_from_nat`, the nat → 32-byte inverse) | `specFnToBoole` emits Boole's native choose-function declaration (`function f(args) : R := choose v : T :: pred;`, Strata #1365), which Strata lowers to an uninterpreted `f` + a choice axiom. Multi-binder chooses normalize to a single product binder first (`normalizeChooseProduct`); a fixed-size-array binder folds its `Sequence.length == N` fact into the predicate so the chosen value carries its `[T; N]` type; the predicate is translated under the (params, v) de Bruijn context the construct's scope chain expects. Soundness: Strata's lowering is unguarded — sound for predicates with a witness at every argument (the general guard is requested on #1365) |
| Trait associated-type projection `<Self as Trait>::Assoc` (parsed into `Typ.Struct` by `Parser.lean`, since VLIR reuses `Struct` for it) reached BooleDDM as an undeclared nominal type — `Undeclared type or category Ops_Arith_mul_Output` for `<&MontgomeryPoint as Mul<&Scalar>>::Output` from the `MulSpecImpl`/`Mul` trait machinery | B5 (only Dalek target using an operator trait with `MulSpecImpl`) | Verus already resolves the impl's return type concretely (`impl&%13::mul`, `impl&%12::mul_spec` both return `MontgomeryPoint`). The parser now records each `TraitMethodImpl`'s implemented method (`kind.TraitMethodImpl.method`, new `traitImplMethod?` field on `SpecFn`/`ExecFn`); `declsToBooleProgram.buildAssocTypeResolution` pairs each abstract `TraitMethodDecl` whose return is a projection-`Struct` with its impl, mapping the projection's Boole name (`Ops_Arith_mul_Output`) to the impl's concrete return type. `typToBooleType` consults this `assocTypeResolution` map (on `BuildCtx`) at the single type-lowering chokepoint, so the projection lowers to `montgomeryPoint`. Guard: only carriers no declared type backs (and not `Vec`) are rewritten, so a real datatype a trait method returns is never hijacked. Assumes one impl per trait method (true for these operator traits). The map also resolves existing `View::V` / `DeepView::V` projections in the BST-map and exec-spec examples; those tests remained stable in the all-suites refresh. |
| Spec-level trait calls carry `resolved_method = null` (the requires/ensures an impl inherits from its trait), so `Impl__13_mul`'s spec referenced the abstract `Std_specs_Ops_mul_req<Self_, Rhs>` — callers faced an uninterpreted predicate, the bodied `Impl__12_*` impls sat unreferenced and got pruned, and the surviving generic vstd decls + the bodiless generic `Ops_Arith_Mul_mul` procedure hit the SMT encoding's generic-fn rejection (the b5 🚨) | B5 (2026-07-03) | `TraitResolve.resolveTraitSpecCalls` — pure VLIR pass before pruning: an `Exp.Call` to an abstract trait method with a *unique* in-program `TraitMethodImpl`, at fully concrete type args, resolves to that impl; `pruneUnreferencedTraitMethodDecls` then drops bodiless abstract trait-method exec decls nothing references (`ExecFn.isTraitMethodDecl`, parsed from `kind`) |
| A spec fn whose body is vstd's `arbitrary()` lowered the call verbatim — `Impl__12_mul_spec { Pervasive_arbitrary }` dangled (the helper is never declared) | B5 (unmasked by the resolution pass keeping `Impl__12_*`) | `bodyIsBareArbitrary`: such fns lower declaration-only — same semantics (unspecified value), no dangling reference |
| Exec-fn direct-`[T; N]` params carry their length only as a body entry-`assume`, but spec-clause definedness is checked spec-level — `clamp_integer`'s own `ensures result == spec_clamp_integer(bytes)` couldn't see `length(bytes) == 32` (7 of b5's 9 ❓) | B5 | Exec fns also emit `ensures Sequence.length(p) == N` per array param, before the user spec: the entry-`assume` discharges it at exit (params are never reassigned), callers pick the fact up for their argument, and — unlike a `requires` — call sites passing unsized-slice elements (`&blocks[k]`) carry no new obligation |
| Spec-fn `recommends` were dropped at parse (they arrive as the spec fn's `decl.reqs`), so `bits_be_as_nat(bits: &[bool], len: int)`'s `0 <= len <= bits.len()` never reached Boole and the body's `bits[len-1]` select-bound was unprovable | B5 | `SpecFn.recommends` parsed from `decl.reqs`; *bodied* spec fns emit them as `requires` (domain conditions — Boole checks body definedness where Verus only hints); body-less fns skip them (no body to check, callers would only gain obligations) |
| A monomorphic spec fn returning `[T; N]` stated its length nowhere (functions have no named return to `ensures` over) — `u8_32_as_nat(spec_clamp_integer(bytes))` definedness had no source for the inner call's length | B5 `mul_clamped`; B1 `mul_return` selects | Per-fn axiom `[<name>_ret_len]: ∀ params :: <param length facts> ==> Sequence.length(<name>(params)) == N`, emitted after the fn's decl.  The guard keeps the axiom a theorem of the defining equation for every body shape: the fn is total in the encoding, so a body returning a (modified) copy of its input — result length equal to the input's — refutes the unguarded form off-domain, making the axiom set unsatisfiable.  (The current b1/b5 array-returning bodies are fixed-arity literals, for which even the unguarded form happens to be a theorem — the guard removes the dependence on that luck.) |
| `low_bits_mask` is uninterpreted and its value lives only in lemma `ensures` unreachable from definition-level obligations — `l51_bit_mask_lt`'s `as_uint(mask51) == toInt(low_bits_mask(51))` was unprovable (b1_full's sole ❓) | b1_full | Ground value axioms per *literal* exponent the program applies the fn to (`axiom [low_bits_mask_51_val]: nat.toInt(Bits_low_bits_mask(nat.fromInt(51))) == 2^51 - 1`), recorded at the `expToBoole` call site so inert axioms are never emitted |
| Global per-field length axioms on total datatypes — `forall s : <dt> :: length(<dtor>(<dt>..<field>(s))) == N` — are refuted by a constructor application of a wrong-length sequence (`edwardsPoint_ctor(Sequence.empty, …)` gives `0 == 5`): the axiom set had no model, so any verdict rested on the solver missing the inconsistency | external review (Codex), 2026-07-04 | Axioms removed; `componentLenFacts` recurses through monomorphic single-constructor struct fields instead, so the same selector-path facts ride on per-boundary requires/ensures (`structFieldInfo` pre-pass); `<fn>_ret_len` axioms guarded by the params' length facts for the same reason; enum payloads recurse the same way under a variant-tester guard (`enumFieldInfo` pre-pass — `isOption_option_Some(r) ==> length(…..payload(r)) == N`), closing the decompress-postcondition class the removal exposed |
| `expMentionsVar` counted *shadowed* occurrences as uses: a spec-level `let`-binder named like a statement local (Verus renumbers both `tmp%%` expression temps and shadowing stmt rebindings to `<name>_ren<id>`, so the names can collide) made `filterLocalsByUse` keep a dead local — whose scaffolding-pruned type (`Option_option`) then dangled (`Undeclared type or category`) | gate `demo_for` under `-V new-mut-ref` exports (the iterator desugar leaves a dead `Option<usize>` stmt local named `tmp` + renumbering) | Binder arms of `expMentionsVar` (Normalize.lean) now test *free* occurrences, mirroring `substExp`'s scope shapes: `Let` rhs outside the binding, `Quant`/`Lambda`/`Choose` binders scoping over body (and `pred`); dalek outputs byte-identical |
| Synthetic loop-exit labels `loop_N` collide with the blocks Strata's `LoopElim` transform synthesizes (also `loop_N`): the post-transform type check rejects label shadowing, so any emitted `loop_N:` block enclosing a while loop fails (`Block label "loop_0" shadows an enclosing block`) | gate `demo_while`/`FindMax` under `-V new-mut-ref` exports (the new while desugar is `while(true)` + conditional `break`, which needs the labeled wrapper; the pattern was latent — only generation-only tests carried it before) | `implicitLoopLabel` emits `vloop_N`, outside `LoopElim`'s `loop_N`/`loop_havoc_N` namespace; upstream fix requested (see Strata table) |

## Open translator gaps (current B1/B3/B4/B5 blockers)

These are feature gaps, not coercion bugs — each needs a designed encoding.

| B | First error | Root cause |
|---|-------------|------------|
| 1 | *(resolved — harness-side)* `b1_full.lean` elaboration overflowed the default thread stack | Lean's `#strata` elaboration of the giant `lemma_mul_value` / `lemma_u64_5_as_nat_product` terms recurses deeply; `run_boole_wrapper` runs `lake env lean -s 131072` (128 MB thread stack), and b1_full elaborates and verifies (z3 2026-07-03: 1237 obl, 1 ❓). |
| 1 | *(resolved 2026-07-03 — translator-emitted)* Definition-level obligations needed ambient facts | Both prototype halves now emit mechanically: the length half via the `Sequence.length` contract set (spec-fn/proof-fn param `requires`, wrapper destructor facts, `*_ret_len` axioms for `[T; N]`-returning spec fns — `mul_return` included), and the ground-value half via per-literal `low_bits_mask` axioms recorded at the `expToBoole` call site (`axiom [low_bits_mask_51_val]`). z3 2026-07-03: b1_minimal 362/397 0 ❓, b1_boundary 501/552 0 ❓, b1_full 1147/1237 with the sole ❓ (`l51_bit_mask_lt`) targeted by the ground axiom — the axiom now sits directly after the fn declaration (axiom scope is positional; the first end-of-program placement was provably inert), z3 re-measure pending; the remaining ⌛ are the nonlinear `Impl__3_mul` chains. |
| 3 | *(resolved 2026-06-10 — B3 is now fully unblocked: cvc5 430/509)* | The two former entries (flat `Tuple_4_k` selectors; tuple-struct destructor naming) were translator bugs, not feature gaps — see the fixed-bugs table (which also covers the `Tuple2` rename) and the Strata table's entry A (StrataBoole type-arg read-back, fixed locally). B3's residual ⌛ are the definition-level `Sequence.select`/pow2 ambient-facts class shared with B1. |
| 4 | *(resolved 2026-06-11 — B4 type-checks fully clean; cvc5 532/667)* | The `choose`-defined spec fn was a translator gap, now closed by emitting Boole's native `command_choosefndef` (Strata #1365) — see the fixed-bugs table. |
| 5 | *(resolved 2026-07-03 — trait-call resolution pass; earlier: type-check resolved 2026-06-14)* | The associated-type blocker was a translator gap, closed by resolving `<&MontgomeryPoint as Mul<&Scalar>>::Output` to the concrete impl type — see the fixed-bugs table. The two verification-faithfulness gaps that remained are closed by `TraitResolve.resolveTraitSpecCalls` (see fixed-bugs table): spec-level calls carrying `resolved_method = null` (the requires/ensures an impl inherits from its trait, e.g. `Impl__13_mul`'s `requires mul_req(self, rhs)`) now resolve to the *unique in-program* `TraitMethodImpl` at concrete type args, so `Impl__12_mul_req` (bodied: `is_valid && rhs.bytes[31] <= 127`) and `Impl__12_obeys_mul_spec` (`false` — the conditional `mul_spec` ensures is provably vacuous) are referenced, kept, and provable at call sites; the now-unreferenced abstract `Std_specs_Ops_*` generics and the bodiless `Ops_Arith_Mul_mul` procedure (whose generic obligations the SMT encoding rejects) are pruned (`pruneUnreferencedTraitMethodDecls`). `mul_clamped`'s call chain verifies: z3 278/281. |

Maps to `BooleBenchmarks.md` gaps: #13 (struct fields, B3/B4), #14 (`Option` —
modeled through the tuple return, B3), #15 (byte arrays, B2/B5), pair returns +
field-op axioms (B4), Montgomery ladder axioms (B5). Gaps #10 (abstract `nat`)
and #11 (recursive spec fns) are handled by the nat model + admitted axioms.

## Strata / StrataBoole bugs found (upstream handoffs)

| Issue | Where | Status |
|-------|-------|--------|
| Polymorphic `mod` leaks the as-int cast's type var (`modt_expr` fvar) — surfaces at `Verify.lean` bvWidth | `BooleDDM` mod type inference | Written up in `…/project/strata-boole-mod-fvar-issue.md` for the Boole developer |
| `LoopElim` synthesizes its loop-wrapper blocks in user-writable label namespace (`.block s!"loop_{n}"` at `Strata/Transform/LoopElim.lean:204`, `loop_havoc_{n}` at `:137`), so a source program whose own block around a while loop is labeled `loop_0` fails the post-transform type check (`Block label "loop_0" shadows an enclosing block`, the shadow rule from upstream 7a5781caf) | `Strata/Transform/LoopElim.lean` | Dodged translator-side (`vloop_N` labels — see fixed-bugs table). Upstream fix: generate the labels like `StructuredToUnstructured.lean:114` does (`StringGenState.gen "loop_entry$"` — the `$` suffix keeps generated labels unwritable), or guarantee freshness against the procedure's existing labels |
| **(A — fixed locally 2026-06-10, PR-ready)** DDM stores type-application argument arrays *reversed* (`flattenTypeApp` pushes the outermost arg first — longstanding convention; Core's own reader compensates with `.reverse`, `DDMTransform/Translate.lean:252,291`), and StrataBoole's `toCoreMonoType` `.fvar` arm missed the restore — every parsed `Tuple A B` reached the verify-time checker as `Tuple B A` while TypeFactory selector signatures use source order, so `Tuple.._0` *behaved* as returning the second component (B3: `Impossible to unify (arrow (Tuple … …) bool) with (arrow choice …)`). Invisible below 2 type args and at symmetric instantiations. | `StrataBoole/Verify.lean:243` | One-line `.reverse` mirroring Core's reader + regression test `StrataBooleTest/datatype_two_type_params.lean` (asymmetric `Pair bool int`, both selectors into typed sinks, passes type-check→encoding→cvc5). Carried in the `pr/casts-boole` working tree; handed to the StrataBoole author as a doc + `git am`-ready patch (`…/project/strata-boole-fix-for-kondylidou.md`, `…/project/strata-boole-tuple-typearg-order.patch`) rather than a PR, since the package lives in their open #1218. Full writeup: `…/project/strata-boole-generic-selector-issue.md` |
| **(B — open)** `@[declareDatatype]` template-generated selector signatures carry named `.tvar`s that DDM elaboration passes through rigidly instead of instantiating; two selector results meeting in one operator fail elaboration (`crypto_noref.lean:76` `Expression has type T1 when T0 expected` on `fun kv : (Tuple2 bv8 bv8) => Tuple2.._0(kv) ^ Tuple2.._1(kv)` — symmetric type, so distinct from A). Selector-into-concrete-context passes and now works end to end. | StrataDDM `@[declareDatatype]` templates + `unifyTypes` tvar pass-through (Elab/Core.lean) | Characterized in the same writeup; keeps `crypto_noref` the 1 known suite fail. Needs selector applications to instantiate type vars at elaboration (or selectors lowered through the dialect `fn` mechanism) |
| **(C — dodged translator-side)** cvc5 reserves `Tuple` as a builtin sort (lenient parsing): emitted `(declare-datatype Tuple (par …))` parses but sort applications resolve to the *builtin*, failing every selector application at the solver. `--strict-parsing` fixes the shadowing but rejects Strata's `is-C` tester syntax (`Op.lean:324`, pre-2.6). | cvc5 builtin-name shadowing × Strata SMT emission | Translator emits `Tuple2` (see fixed-bugs table). Strata recommendations (in the writeup): emit standard `(_ is C)` testers; mangle user sorts colliding with solver builtins via the `findUnique` registry |
| `gen_smt_vcs` drops the bitvector width in `ubv_to_int`/`sbv_to_int` (`BitVec.toNat` applied without the width) | `Strata/DL/SMT/Translate.lean` | Fixed in cslib's vendored Strata copy (recover width via `getBitVecWidth`); dev repo + upstream still unfixed — PR-worthy |
| `gen_smt_vcs` crashes on opaque `datatype` wrappers (`withTypeDecls` declares the sort, `translateSort` can't find it) | `Strata/DL/SMT/Translate.lean` | Known; deferred to Strata devs. Forces the synonym encoding, which in turn makes cvc5 unfold recursive spec fns and time out — the two backends pull on opposite encodings |
| `String.IsSuffix` defined in the global `String` namespace collides with Mathlib (`Mathlib.Data.String.Defs`) — blocks importing `nlinarith` alongside Strata | `Strata/DL/Util/StringGen.lean` | Found; rename is PR-worthy (used only inside `StringGen.lean`) |
| `gen_smt_vcs` is slow on large programs (~8 min on B2): `nativeDecide` over the whole VC set as one giant literal, recompiled every elaboration | `MetaVerifier.lean` | Documented; mitigations: `#exit` while editing, shrink program terms |

## Verification-level wall (B1 family)

With translation clean, B1's bottleneck is **nonlinear integer arithmetic**
(25 limb products, carry-chain bounds), not pow2 modeling (uninterpreted
`pow2` + imported value/monotonicity lemmas is correct and not the limiter):

- cvc5 (`Boole.verify`): 157/282 on the sound synonym encoding; the 125
  timeouts are transparent-synonym unfolding + nonlinear grinding.
- cslib Lean backend (`gen_smt_vcs; grind|omega|smt`): 61/70; the 9 open VCs
  are products-of-bounded-limbs goals (`nlinarith`-shaped; blocked from trying
  `nlinarith` by the `String.IsSuffix` clash above).
- Levers, in rough order: make the heavy spec fns opaque for cvc5; fix the
  `gen_smt_vcs` datatype crash (enables the cvc5-friendly opaque encoding in
  both backends); prove the 9 residuals with nonlinear tactics once Mathlib
  can be imported.
